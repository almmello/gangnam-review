# Never prefix secrets with NEXT_PUBLIC_. Store production secrets in Vercel.
DEEPSEEK_API_KEY=
# Production sharing is opt-in and consumes the owner's provider balance.
ENABLE_SHARED_AI=false
# Models are pinned in src/lib/analysis/providers.ts; no arbitrary model override.
